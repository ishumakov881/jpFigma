/*
===============================================================================
UNIVERSAL VIEWMODEL + JETPACK COMPOSE GUIDELINE
Clean Architecture · MVVM · StateFlow
===============================================================================

ЦЕЛЬ
-----
Приводить ЛЮБУЮ ViewModel (Login, Profile, List, Details и т.д.)
к предсказуемой, масштабируемой и UI-независимой архитектуре.

-------------------------------------------------------------------------------
❌ ЗАПРЕЩЕНО (КРИТИЧЕСКИЕ ОШИБКИ)
-------------------------------------------------------------------------------

❌ UI читает поля ViewModel напрямую
viewModel.errorMsg
viewModel.isLoading
Причина:

- это не state
- Compose не контролирует жизненный цикл
- возможны дубли и баги при recomposition

❌ Side-effects в теле Composable
Toast.makeText(...)
navController.navigate(...)
Причина:

- side-effect повторяется при каждой recomposition

❌ mutableStateOf внутри ViewModel
Причина:

- ViewModel становится зависимой от Compose

❌ String как ошибка в ViewModel
errorMsg: String?
Причина:

- ViewModel не должна знать, что и как показывать пользователю

❌ Любой UI-код во ViewModel
Context
Toast / Snackbar
stringResource
MaterialTheme
println для UI-событий

-------------------------------------------------------------------------------
✅ ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА
-------------------------------------------------------------------------------

1️⃣ ЕДИНЫЙ ИСТОЧНИК ИСТИНЫ — UiState

ViewModel ОБЯЗАНА иметь:
val uiState: StateFlow<UiState>

UI ОБЯЗАН читать ТОЛЬКО его:
val state by viewModel.uiState.collectAsState()

-------------------------------------------------------------------------------

2️⃣ ВСЁ СОСТОЯНИЕ ЭКРАНА — В UiState

Пример:
data class UiState(
val isLoading: Boolean = false,
val error: UiError? = null
)

❌ Никаких отдельных полей в ViewModel

-------------------------------------------------------------------------------

3️⃣ ОШИБКИ — ТОЛЬКО ТИПИЗИРОВАННЫЕ

Разрешено:
sealed interface UiError

Запрещено:
String
message в ViewModel

-------------------------------------------------------------------------------

4️⃣ ViewModel НЕ ФОРМИРУЕТ ТЕКСТ

❌ Нельзя:
errorMsg = "Неверный пароль"

✅ Можно:
error = UiError.Unauthorized

UI сам решает, КАК это отобразить

-------------------------------------------------------------------------------

5️⃣ try/catch — ТОЛЬКО В ViewModel

- UI НИКОГДА не ловит исключения
- Все Throwable маппятся в UiError

Принцип:
Throwable → UiError → UiState → UI

-------------------------------------------------------------------------------

6️⃣ ОБНОВЛЕНИЕ STATE — ТОЛЬКО ЧЕРЕЗ copy

❌ Нельзя:
isLoading = true
errorMsg = null

✅ Можно:
_uiState.update {
it.copy(isLoading = true, error = null)
}

-------------------------------------------------------------------------------

7️⃣ SIDE-EFFECTS — ТОЛЬКО ЧЕРЕЗ LaunchedEffect

Разрешено:
LaunchedEffect(state.error) {
// toast / navigation
}

Запрещено:
вызывать side-effect из тела Composable

-------------------------------------------------------------------------------
🧪 CHECKLIST (ПРОВЕРКА ГОТОВНОСТИ)
-------------------------------------------------------------------------------

ViewModel считается ПРАВИЛЬНОЙ, если:

[ ] UI не обращается к полям ViewModel напрямую
[ ] Есть ровно один UiState
[ ] Используется StateFlow
[ ] Нет mutableStateOf в ViewModel
[ ] Ошибки — sealed class/interface
[ ] Нет UI-кода во ViewModel
[ ] Side-effects не вызываются из Composable body

-------------------------------------------------------------------------------
🧠 КОРОТКАЯ ФОРМУЛА
-------------------------------------------------------------------------------

State → UiState (StateFlow)
UI → читает state
Effects → LaunchedEffect
ViewModel → без UI

===============================================================================
КОНЕЦ GUIDELINE
===============================================================================
*/
